# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: dreamcodez/dead-simple-nodejs@1.0.1
        with:
            # nodejs version to install
            NODE_VERSION: 14.18.1
            # nodejs distro to install
            NODE_DISTRO: linux-x64
      - run: npm install @angular/cli & npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEVELOPERGPT_C5819 }}'
          channelId: live
          projectId: developergpt-c5819
